---
title: "ERC-20 vs LSP7"
left: { chip: "ERC-20", label: "fungible token" }
right: { chip: "LSP7", label: "Digital Asset" }
summary: "ERC-20 made fungible tokens composable on EVM. LSP7 keeps the model, then adds receiver hooks, transfer context, scoped operators, and ERC-725Y-backed metadata."
quotableAnswer: >-
  ERC-20 is Ethereum's fungible token minimum interface: six functions, two events, no receiver hook, no structured metadata, no per-transfer context. LSP7 on LUKSO keeps the mental model but adds LSP1 `universalReceiver` hooks on both sides of every transfer, a `data` payload for transfer context, operator authorization scoped through LSP6 Key Manager permissions at the account, and LSP4 metadata under ERC-725Y typed keys instead of separate `name()`/`symbol()` calls. Same conceptual role, less integration tax on top.
updated: 2026-06-22
verdict:
  useLeft: "When the minimum-interface property is the design goal. Pull payments via approve / transferFrom, fixed metadata of name / symbol / decimals, and zero recipient code execution on transfer are features. Contracts that ABI-check for ERC-20 selectors only dispatch to ERC-20."
  useRight: "When the token needs to carry transfer context (the bytes data payload), trigger recipient logic via LSP1, expose structured ERC-725Y metadata under LSP4 keys, or be authorized through an LSP6-gated account controller instead of a raw allowance."
  neither: "If reentrancy from recipient code is unacceptable in your protocol, ERC-20 is the safer interface — no recipient call exists to reenter."
matrix:
  - row: "balance model"
    left: "balanceOf(address) → uint256"
    right: "balanceOf(address) → uint256"
  - row: "transfer signature"
    left: "transfer(to, amount)"
    right: "transfer(from, to, amount, force, data)"
  - row: "recipient notification"
    left: "none"
    right: "LSP1 universalReceiver on both sides"
  - row: "transfer data payload"
    left: "none — workaround via wrappers"
    right: "bytes calldata data — native"
  - row: "authorization"
    left: "approve / allowance / transferFrom"
    right: "authorizeOperator + LSP6 controller scope"
  - row: "metadata"
    left: "name / symbol / decimals only"
    right: "ERC-725Y under LSP4 keys"
  - row: "metadata shape"
    left: "off-chain by convention"
    right: "typed JSON via LSP4, VerifiableURI optional"
  - row: "interface compatibility"
    left: "ERC-20 selectors — code that ABI-checks for ERC-20 dispatches"
    right: "different selectors — won't accidentally satisfy ERC-20 ABI checks"
  - row: "reentrancy surface on transfer"
    left: "none — no recipient call"
    right: "LSP1 hook fires on recipient; standard reentrancy guards required"
docs:
  - label: "docs.lukso.tech / LSP7 Digital Asset"
    href: "https://docs.lukso.tech/standards/tokens/LSP7-Digital-Asset/"
  - label: "docs.lukso.tech / migrate ERC-20 to LSP7"
    href: "https://docs.lukso.tech/learn/migrate/migrate-erc20-to-lsp7/"
related: ["problems/erc20-transfer-hooks", "problems/erc20-approval-risks", "migrate/erc20-to-lsp7"]
---

## How they differ structurally

ERC-20 is a balance sheet plus a transfer event. The minimum interface that made composability
possible. The integration tax — receiver hooks, transfer context, approval risk — falls on
everyone who builds on top.

LSP7 keeps the balance sheet and adds the surrounding system. Every transfer has a `force`
flag and a `bytes data` payload. Every transfer fires `universalReceiver` on sender and
recipient through LSP1. Every authorization event includes an `operatorNotificationData`
payload so receivers can react when operators are set or revoked. Metadata lives in ERC-725Y
under LSP4-defined keys, not in three top-level functions.

## When to migrate

Migrate when the token needs receiver awareness (vaults that credit on deposit), structured
metadata (governance tokens with proposal links), or account-level permission scoping (apps
that should have narrow authority, not blanket allowance).

Don't migrate when ERC-20 compatibility is the product. A wrapped reserve asset doesn't gain
from LSP7's hooks.

## Operator scope is honest

LSP7 authorizeOperator is still amount-scoped. It is not magical. The structural shift comes
from pairing it with a Universal Profile: now the *controller* calling `authorizeOperator` is
itself constrained by LSP6 permissions, allowed calls, and allowed selectors. The token
contract doesn't have to police future drains — the account does, at a higher altitude.
