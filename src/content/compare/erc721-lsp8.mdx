---
title: "ERC721 vs LSP8"
left: { chip: "ERC·721", label: "identifiable NFT" }
right: { chip: "LSP·8", label: "Identifiable Digital Asset" }
description: "ERC721 standardized NFT ownership. LSP8 keeps the ownership model, then upgrades token IDs to bytes32, replaces tokenURI with typed per-token data, and routes every transfer through LSP1."
updated: 2026-06-22
verdict:
  useLeft: "Marketplace compatibility is the product. If OpenSea, Blur, and every wallet that already speaks ERC721 are the integration surface, ERC721 wins by reach."
  useRight: "Token IDs need to mean something, or metadata is dynamic, or the asset is part of a profile-aware app. LSP8 was designed for those cases."
  neither: "A static, immutable, fully on-chain SVG art collection — ERC721 is fine and the LSP layer adds no value."
matrix:
  - row: "token ID type"
    left: "uint256"
    right: "bytes32"
  - row: "id semantics"
    left: "numeric / counter"
    right: "number, hash, serial, encoded reference"
  - row: "metadata pointer"
    left: "tokenURI(id) → string"
    right: "getDataForTokenId(id, key) → bytes"
  - row: "dynamic updates"
    left: "MetadataUpdate event (ERC4906) — signal only"
    right: "setDataForTokenId — typed write"
  - row: "transfer hook"
    left: "onERC721Received (only on safeTransferFrom)"
    right: "universalReceiver (LSP1) on every transfer"
  - row: "transfer data payload"
    left: "bytes _data (only on safeTransferFrom)"
    right: "bytes data on every transfer + force flag"
  - row: "off-chain integrity"
    left: "trust the host"
    right: "VerifiableURI option in LSP4"
  - row: "marketplace reach"
    left: "every NFT venue today"
    right: "LUKSO marketplaces; standard is plain EVM, deployable anywhere"
docs:
  - label: "docs.lukso.tech / LSP8 Identifiable Digital Asset"
    href: "https://docs.lukso.tech/standards/tokens/LSP8-Identifiable-Digital-Asset/"
  - label: "docs.lukso.tech / migrate ERC721 to LSP8"
    href: "https://docs.lukso.tech/learn/migrate/migrate-erc721-to-lsp8/"
related: ["problems/erc721-dynamic-metadata", "problems/erc721-tokenid-limits", "problems/erc721-safe-transfer"]
---

## How they differ structurally

ERC721 is `ownerOf(tokenId)` plus a transfer event, with `tokenURI(tokenId)` returning a
string. Everything else — dynamic metadata, receiver behavior, marketplace cooperation — is
convention layered on top.

LSP8 keeps `ownerOf`-equivalent behavior but widens the token ID to `bytes32` and replaces
`tokenURI` with a typed per-token key-value store. Want a content hash as the ID? Store the
hash. Want per-token attributes that change at mint, on reveal, on a game event? Set the data
key, gated by whatever permissions you've configured on the asset contract.

## What changes for marketplaces

Existing mainnet NFT marketplaces don't speak LSP8 today — that's the real constraint. The
standard itself is EVM-compatible and could ship anywhere, but adoption is what makes a
marketplace useful, and the LSP-aware marketplace ecosystem lives on LUKSO. If the collection
needs to list on the established mainnet NFT venues right now, ERC721 is the path. If the
collection lives in an app or a profile-native marketplace on LUKSO, LSP8 gives the asset
richer self-description without sidecar APIs.

## bytes32 is the small detail that matters

A `uint256` token ID is fine when IDs are sequential counters. The moment IDs need to *mean*
something (a hash of mint inputs, a serial keyed to off-chain state, a reference encoded with
its provenance), `uint256` forces a side mapping. `bytes32` makes the ID itself the data
structure.
