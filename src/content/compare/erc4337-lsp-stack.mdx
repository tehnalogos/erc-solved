---
title: "ERC-4337 vs the LSP account stack"
left: { chip: "ERC-4337", label: "account abstraction over EOAs" }
right: { chip: "LSP·0+6+20+25", label: "native smart account" }
description: "ERC-4337 puts account abstraction on top of EOAs via a parallel mempool, EntryPoint, and bundlers. The LSP stack puts the account contract on the normal call path, with permissions and relay execution as composed standards."
updated: 2026-06-22
verdict:
  useLeft: "When you need account abstraction to layer onto a chain whose native transaction model is EOA-anchored, with pluggable per-account validators and a separate aggregation/sponsorship plane that's decoupled from the account itself."
  useRight: "When you want the account contract to be the call entry point — no EntryPoint, no UserOperation envelope, no bundler. `msg.sender` at downstream targets is the account; LSP6 vocabulary expresses permissions; LSP25 handles sponsored execution on the account itself."
matrix:
  - row: "transaction flow"
    left: "user → UserOp pool → bundler → EntryPoint → account"
    right: "user (controller) → account contract"
  - row: "permission layer"
    left: "validator module (per-account)"
    right: "LSP6 Key Manager (standardized)"
  - row: "gas sponsorship"
    left: "paymaster (separate contract)"
    right: "LSP25 executeRelayCall (on the account)"
  - row: "signature verification"
    left: "validateUserOp on the account"
    right: "LSP20 lsp20VerifyCall inline"
  - row: "explorer experience"
    left: "shows handleOps + UserOp decode"
    right: "shows the call as itself"
  - row: "trace / debug surface"
    left: "appears as handleOps → EntryPoint → account (extra hops)"
    right: "appears as a direct call into the account contract"
  - row: "permission vocabulary"
    left: "per-account validator modules — non-standard across accounts"
    right: "LSP6 permission bitfield + AllowedCalls + AllowedERC725YDataKeys — uniform"
docs:
  - label: "docs.lukso.tech / LSP0 ERC-725 Account"
    href: "https://docs.lukso.tech/standards/accounts/lsp0-erc725account/"
  - label: "docs.lukso.tech / LSP25 executeRelayCall"
    href: "https://docs.lukso.tech/standards/accounts/lsp25-execute-relay-call/"
related: ["problems/erc4337-bundler-tax", "problems/gasless-onboarding", "problems/wallet-permissions"]
---

## Two answers to the same question

Both stacks deliver "smart account UX": fine-grained permissions, sponsored execution,
account-level signature verification. They differ on *where* that work happens.

ERC-4337 ships account abstraction over a chain whose primitive is the EOA. The cost is a
parallel infrastructure (UserOperation pool, bundlers, EntryPoint, paymasters) sitting beside
the normal transaction flow. The benefit is portability — the same account model works
anywhere EntryPoint is deployed.

The LSP stack puts the account contract on the regular call path. There's no UserOperation,
no EntryPoint, no bundler — the controller calls the account, LSP20 inline-verifies via
LSP6, the call executes. Gasless flows go through LSP25 on the account itself, not through a
separate paymaster contract.

## Which model fits the product

If your product needs account abstraction that's *decoupled* from the underlying account
contract — pluggable validator modules per account, a separate aggregation/sponsorship plane,
the option to mix-and-match validators per UserOp — ERC-4337's parallel infrastructure is the
mechanism. The cost is the extra hops in every trace and the non-standard permission shape
each validator defines.

If your product needs the account itself to be the call entry point — so downstream contracts
see the account as `msg.sender`, permissions live in a standard on-chain vocabulary, and
sponsored execution is a function on the account contract rather than a separate paymaster —
the LSP stack is the mechanism. The cost is that account behavior is fixed by the contract;
adding new authorization shapes means extending through LSP17, not swapping a validator.
